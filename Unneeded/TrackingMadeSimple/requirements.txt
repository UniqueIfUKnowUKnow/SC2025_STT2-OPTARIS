# Requirements for Drone Detection and Tracking System
# Raspberry Pi dependencies

# Core system libraries
RPi.GPIO>=0.7.0
pigpio>=1.78
pyserial>=3.5

# Scientific computing
numpy>=1.21.0
scipy>=1.7.0

# Astronomical calculations (for TLE processing)
ephem>=4.1.4

# Data processing and output
pandas>=1.3.0

# Optional: For advanced plotting and visualization
matplotlib>=3.4.0

# Optional: For machine learning enhancements
scikit-learn>=1.0.0

# Development and testing
pytest>=6.2.0
pytest-mock>=3.6.0

# Note: Some packages may need to be installed via apt on Raspberry Pi:
# sudo apt-get update
# sudo apt-get install python3-pip python3-dev
# sudo apt-get install pigpio python3-pigpio
# sudo apt-get install python3-rpi.gpio
